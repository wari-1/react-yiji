const initialState = {
  yindaoyeImages: [
    {
      id: "hsh",
      src: ""
    }
  ]
};
export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
